general:
  models_dir: "<path to model checkpoints folder>" # we are using models trained in https://github.com/locuslab/smoothing/tree/master
  dataset_dir: "<path to dataset folder>" # we are using CIFAR-10 dataset
  logits_dir: "<where you want to store the smooth logits>" # we are using logits generated by the models in

models:
  cifar10: resnet110